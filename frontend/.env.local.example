# Base URL of the OpsMind FastAPI backend's v1 API.
# The browser calls ONLY this URL - it never calls Groq/OpenAI/etc directly,
# and no LLM provider key is ever present in frontend code or env vars.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
